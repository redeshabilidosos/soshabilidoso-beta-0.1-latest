# 📦 Instalación de Emoji Picker

Ejecuta este comando para instalar el emoji picker:

```bash
npm install emoji-picker-react
```

O si usas yarn:

```bash
yarn add emoji-picker-react
```

Luego continúa con los componentes actualizados.
